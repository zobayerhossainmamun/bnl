// Back-compat shim — http.serve(port, handler) is preserved.
// New code should prefer web.serve({port, tls?}, handler), which also covers HTTPS.
import "web" as web;

function serve(port, handler) {
    return web.serve({port: port}, handler);
}

// Bilingual alias.
var চালাও = serve;
