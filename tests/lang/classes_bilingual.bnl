// Class declared with the Bangla keyword `শ্রেণী`, methods with `ফাংশন`,
// fields and method names written in Bangla. The whole vocabulary is bilingual.
শ্রেণী হিসাব {
    ফাংশন init(self, প্রারম্ভ) {
        self.মান = প্রারম্ভ;
    }
    ফাংশন যোগ(self, n) {
        self.মান = self.মান + n;
    }
    ফাংশন পান(self) {
        ফেরত self.মান;
    }
}

চলক h = হিসাব(7);
লিখুন("start:", h.পান());
h.যোগ(3);
h.যোগ(5);
লিখুন("after:", h.পান());
লিখুন("type:", ধরণ(h));
